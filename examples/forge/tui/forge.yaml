# Forge blueprint for a TUI chat agent.
# Run with:
#   cd examples/forge/tui
#   go run ../../../cmd/forge -config forge.yaml
# The output binary is written relative to the current working directory.
dist:
  name: tui-chat
  output_path: ./tui-chat
conduits:
  - name: tui
    module: github.com/andrewhowdencom/ore/x/conduit/tui
# Uncomment to inject a system prompt and guardrails into the inference context:
# transforms:
#   - module: github.com/andrewhowdencom/ore/x/systemprompt
#     options:
#       content: "You are a helpful assistant."
#   - module: github.com/andrewhowdencom/ore/x/guardrails
#     options:
#       rules:
#         - "Always format code in markdown blocks."
